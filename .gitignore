# Xcode
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
DerivedData/
*.moved-aside
*.xcuserstate
*.xcscmblueprint

# SPM
.build/
.swiftpm/
/Package.resolved
# Build output (swift build / xcodebuild scratch)
/build/
Vendor/*/build/

# macOS
.DS_Store
*.swp
*~

# Dependencies
Packages/

# Subagent-driven execution scratch (ledger, briefs, diffs)
.superpowers/

# Session attachments / local context (never commit — may contain screenshots with account data)
.context/

# Recordings / local data — never commit call audio or databases
*.caf
*.wav
*.m4a
*.mp3
*.store
*.sqlite
*.sqlite-wal
*.sqlite-shm
dist/
